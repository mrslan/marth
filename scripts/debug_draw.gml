//owo

// draw_debug_text( x, y + 20, string(x) + ", " + string(y));

// draw_debug_text( x, y+20, string(djumps));
// draw_debug_text( x, y+40, string(max_djumps));
// draw_debug_text( x, y+60, string(turn_delay));
// draw_debug_text( x, y+80, string(state));

// if (hit_player_obj != -1)
// draw_debug_text( x, y, string(window));

//  draw_debug_text( x, y+20, string(lasermode));